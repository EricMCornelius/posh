require('${POSH}/node_modules/node_extensions');

var fs = require('fs');
var util = require('util');
var async = require('${POSH}/node_modules/async');
var processors = require('${POSH}/lib/registry/registry').processors;
